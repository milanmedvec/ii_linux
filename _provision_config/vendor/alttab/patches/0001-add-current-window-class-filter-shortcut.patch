diff --git a/src/alttab.c b/src/alttab.c
index 00e262d..1b1882f 100644
--- a/src/alttab.c
+++ b/src/alttab.c
@@ -539,6 +539,11 @@ static int grabKeysAtStartup(bool grabUngrab)
          g.ignored_modmask)) {
         die(grabhint, g.option_keyCode, g.option_modMask, g.ignored_modmask);
     }
+    if (!changeKeygrab
+        (root, grabUngrab, 49, g.option_modMask,
+         g.ignored_modmask)) {
+        die(grabhint, 49, g.option_modMask, g.ignored_modmask);
+    }
     if (!changeKeygrab
         (root, grabUngrab, g.option_keyCode,
          g.option_modMask | g.option_backMask, g.ignored_modmask)) {
@@ -633,7 +638,18 @@ int main(int argc, char **argv)
                 if (ev.xkey.keycode == g.option_keyCode) {  // tab
                     CHECK_97;
                     if (!g.uiShowHasRun) {
-                        uiShow((ev.xkey.state & g.option_backMask));
+                        uiShow((ev.xkey.state & g.option_backMask), 0);
+                    } else {
+                        if (ev.xkey.state & g.option_backMask) {
+                            uiPrevWindow();
+                        } else {
+                            uiNextWindow();
+                        }
+                    }
+                } else if (ev.xkey.keycode == 49) {  // tab
+                    CHECK_97;
+                    if (!g.uiShowHasRun) {
+                        uiShow((ev.xkey.state & g.option_backMask), 1);
                     } else {
                         if (ev.xkey.state & g.option_backMask) {
                             uiPrevWindow();
diff --git a/src/alttab.h b/src/alttab.h
index dc15db5..c7f5e74 100644
--- a/src/alttab.h
+++ b/src/alttab.h
@@ -215,7 +215,7 @@ typedef struct {
 
 // gui
 int startupGUItasks(void);
-int uiShow(bool direction);
+int uiShow(bool direction, bool classFilter);
 void uiExpose(void);
 int uiHide(void);
 int uiNextWindow(void);
@@ -233,7 +233,7 @@ int addIconFromHints(WindowInfo * wi);
 int addIconFromFiles(WindowInfo * wi);
 int addWindowInfo(Window win, int reclevel, int wm_id, unsigned long desktop,
                   char *wm_name);
-int initWinlist(void);
+int initWinlist(bool classFilter);
 void freeWinlist(void);
 int setFocus(int winNdx);
 int rp_startupWintasks(void);
@@ -255,7 +255,7 @@ void shutdownWin(void);
 /* EWHM */
 bool ewmh_detectFeatures(EwmhFeatures * e);
 Window ewmh_getActiveWindow(void);
-int ewmh_initWinlist(void);
+int ewmh_initWinlist(bool classFilter);
 int ewmh_setFocus(int winNdx, Window fwin); // fwin used if non-zero
 unsigned long ewmh_getCurrentDesktop(void);
 unsigned long ewmh_getDesktopOfWindow(Window w);
diff --git a/src/ewmh.c b/src/ewmh.c
index ef63400..2ea4f23 100644
--- a/src/ewmh.c
+++ b/src/ewmh.c
@@ -196,13 +196,14 @@ Window ewmh_getActiveWindow(void)
 // initialize winlist, correcting sortlist
 // return 1 if ok
 //
-int ewmh_initWinlist(void)
+int ewmh_initWinlist(bool classFilter)
 {
     Window *client_list;
     unsigned long client_list_size;
     int i;
     Window aw;
     char *title;
+    char *aw_class;
     unsigned long current_desktop, window_desktop;
 
     current_desktop = ewmh_getCurrentDesktop();
@@ -213,13 +214,23 @@ int ewmh_initWinlist(void)
         // continue anyway
     }
 
+    aw_class = get_x_property(aw, XA_STRING, "WM_CLASS", NULL);
+    if (aw_class) {
+        char *first_space = strchr(aw_class, ' ');
+        if (first_space) {
+            *first_space = '\0';
+        }
+    }
+
     client_list = ewmh_get_client_list(&client_list_size);
     if (client_list == NULL) {
+        free(aw_class);
         msg(-1, "can't get client list\n");
         return 0;
     }
 
     for (i = 0; i < client_list_size / sizeof(Window); i++) {
+        char *w_class;
         Window w = client_list[i];
 
         if (ewmh_skipWindowInTaskbar(w))
@@ -229,6 +240,21 @@ int ewmh_initWinlist(void)
         if (common_skipWindow(w, current_desktop, window_desktop))
             continue;
 
+        if (classFilter) {
+            w_class = get_x_property(w, XA_STRING, "WM_CLASS", NULL);
+            if (w_class) {
+                char *first_space = strchr(w_class, ' ');
+                if (first_space) {
+                    *first_space = '\0';
+                }
+            }
+            if (aw_class && w_class && strcmp(aw_class, w_class) != 0) {
+                free(w_class);
+                continue;
+            }
+            free(w_class);
+        }
+
         // build title
         char *wmn1 = get_x_property(w, XA_STRING, "WM_NAME", NULL);
         Atom utf8str = XInternAtom(dpy, "UTF8_STRING", False);
@@ -251,6 +277,7 @@ int ewmh_initWinlist(void)
     //  aw, (g.winlist ? g.winlist[g.startNdx].name : "null"));
 
     free(client_list);
+    free(aw_class);
     return 1;
 }
 
diff --git a/src/gui.c b/src/gui.c
index 0742c5b..0a7a22b 100644
--- a/src/gui.c
+++ b/src/gui.c
@@ -425,7 +425,7 @@ int startupGUItasks(void)
 // returns 1 if our window is ready to Expose, 0 otherwise
 // direction is direction of first press: with shift or without
 //
-int uiShow(bool direction)
+int uiShow(bool direction, bool classFilter)
 {
     msg(0, "preparing ui\n");
     g.uiShowHasRun = true;      // begin allocations
@@ -484,7 +484,7 @@ int uiShow(bool direction)
 // GC,
 // g.vp (for SCR_CURRENT)
 
-    if (!initWinlist()) {
+    if (!initWinlist(classFilter)) {
         msg(0, "initWinlist failed, skipping ui initialization\n");
         return 0;
     }
diff --git a/src/win.c b/src/win.c
index edcb6e9..7061bd2 100644
--- a/src/win.c
+++ b/src/win.c
@@ -574,7 +574,7 @@ static void __initWinlist(void)
 // n.b.: in heavy WM, use _NET_CLIENT_LIST
 // direction is direction of first press: with shift or without
 //
-int initWinlist(void)
+int initWinlist(bool classFilter)
 {
     int r;
     if (g.debug > 1) {
@@ -589,7 +589,7 @@ int initWinlist(void)
         r = rp_initWinlist();
         break;
     case WM_EWMH:
-        r = ewmh_initWinlist();
+        r = ewmh_initWinlist(classFilter);
         break;
     case WM_TWM:
         r = x_initWindowsInfoRecursive(root, 0);
